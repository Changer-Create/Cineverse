# 主分支代码审计（2026-08-22）

## 范围

本次检查覆盖三个页面入口、运行时加载链、全部根目录 JavaScript，以及云登录界面的动态 HTML 输出。仓库是无构建步骤的静态应用，因此 `content-center-runtime-v1.js` 是前台与管理台共享的实际模块清单。

## 已处理问题

### 高风险：云登录错误信息可进入 HTML

Supabase 或网络层返回的错误文本此前直接拼入 `innerHTML`。虽然常见错误会被转换成固定中文提示，未知服务端错误仍可携带 HTML，形成 DOM XSS 注入面。现在所有动态状态文本和账号邮箱在插入模板前统一进行 HTML 转义。

### 中风险：第三方 SDK 使用浮动版本

登录模块此前加载 `@supabase/supabase-js@2`，会在没有代码复核的情况下接受后续 2.x 版本。运行时依赖现已锁定到明确版本，降低上游更新导致的供应链变更与兼容冲突风险。

### 中风险：重复启动层破坏缓存

入口过去先加载 `content-center.js`，再以 `Date.now()` 查询参数写入真正运行时。这会使每次访问都绕过浏览器缓存，并额外依赖一层 `document.write`。两个实际入口现直接加载带固定发布版本的运行时文件。

### 维护风险：多代实现与隔离文件并存

仓库曾同时保留云同步、搜索、筛选、卡片、评分和雷达的旧版本，还有一份超过 400 KB 的 HTML 时间戳快照。它们不在当前加载链中，却容易被误接回页面并造成事件监听器、存储拦截器和渲染器冲突。本次已删除这些废弃实现；Git 历史仍保留完整恢复能力。

## 自动回归门槛

运行 `node scripts/audit-runtime.mjs` 会确认：

1. 页面和运行时清单引用的本地 JS/CSS 全部存在；
2. 已退役实现没有重新出现；
3. 当前所有根目录 JavaScript 均通过 Node 语法检查。

## 仍需部署侧确认

以下项目无法仅凭静态仓库证明，发布前应在 Supabase 控制台和托管平台复核：

- `user_data`、反馈表和截图 bucket 的 RLS 是否按用户隔离；
- Edge Functions 是否校验 JWT、管理员角色、请求体大小与速率限制；
- 托管响应是否配置 CSP、`X-Content-Type-Options: nosniff`、`Referrer-Policy` 和合理的 `Permissions-Policy`；
- 定期使用真实账号演练云端/本地冲突合并、退出登录与跨设备恢复。
